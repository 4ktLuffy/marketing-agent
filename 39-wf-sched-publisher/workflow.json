{
  "id": "mktWf39Publisher",
  "name": "39 · Schedule · Publisher",
  "nodes": [
    {
      "id": "e9ec5713-9a95-5800-b915-46de88ebd0c4",
      "name": "Every 15 minutes",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        300
      ],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "*/15 * * * *"
            }
          ]
        }
      }
    },
    {
      "id": "454f06e6-99b9-50e3-a69c-da582cc825b3",
      "name": "Due items",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        240,
        300
      ],
      "parameters": {
        "method": "GET",
        "url": "={{ $env.CALENDAR_URL }}/due",
        "options": {
          "response": {
            "response": {
              "fullResponse": true
            }
          }
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.INTERNAL_API_KEY }}"
            }
          ]
        }
      }
    },
    {
      "id": "4c68eadf-61dc-5338-a918-ae9d6450627f",
      "name": "One per item",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        480,
        300
      ],
      "parameters": {
        "jsCode": "if (!$env.PUBLISH_WEBHOOK_URL) return [];   // not configured: publish nothing, mark nothing\nconst raw = [].concat($input.first().json.body ?? []);  // full response: the list is in body\nconst all = raw.length === 1 && Array.isArray(raw[0]) ? raw[0] : raw.filter(i => i && i.id);\n// Review replies (60) are never sent anywhere: a person copies the approved reply to\n// Google or Trustpilot by hand.\nconst items = all.filter(i => String(i.channel).toLowerCase() !== 'review_reply');\n// The stored link is the base URL; the text usually carries it with UTM parameters.\n// Shorten the full URL as it appears in the text so tracking survives.\nreturn items.map(i => {\n  // Campaign drafts carry their tracked URL only in the text (link field empty): use the\n  // first URL then, or campaign clicks could never be counted through the short link.\n  const urls = (String(i.body).match(/https?:\\/\\/\\S+/g) || []).map(u => u.replace(/[).,!?]+$/, ''));\n  const inText = i.link ? urls.find(u => u.startsWith(i.link)) : urls[0];\n  const full = inText || i.link || null;\n  // Fill in missing UTM tags (a reviewer may paste a plain link): utm_content = calendar item\n  // id so clicks map back to this exact post; campaign posts also get their campaign tags,\n  // or the campaign's click count would miss them.\n  const MEDIUM = { email: 'email', blog: 'referral', google_ads: 'cpc' };\n  const add = (u, k, v) => !u || !v || new RegExp(`[?&]${k}=`).test(u) ? u : `${u}${u.includes('?') ? '&' : '?'}${k}=${encodeURIComponent(v)}`;\n  let tracked = full;\n  if (i.campaign) {\n    tracked = add(tracked, 'utm_source', String(i.channel).toLowerCase());\n    tracked = add(tracked, 'utm_medium', MEDIUM[i.channel] || 'social');\n    tracked = add(tracked, 'utm_campaign', i.campaign);\n  }\n  tracked = add(tracked, 'utm_content', String(i.id));\n  return { json: { ...i, full_link: full, tracked_link: tracked } };\n});\n"
      }
    },
    {
      "id": "dac4be6a-7797-56df-a375-b9a35712cbde",
      "name": "Short link",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        720,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "={{ $env.SHORTENER_URL }}/links",
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          }
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.INTERNAL_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ url: $json.tracked_link || 'none' }) }}"
      }
    },
    {
      "id": "ad70d67d-52be-58bb-89e3-042836ab10dc",
      "name": "Payload",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        960,
        300
      ],
      "parameters": {
        "jsCode": "const item = $('One per item').item.json;\nconst short = $json.short_url;\nconst text = item.full_link && short ? String(item.body).split(item.full_link).join(short) : item.body;\nreturn { json: { id: item.id, channel: item.channel, title: item.title, text, link: short || item.full_link || null,\n  short_url: short || null, campaign: item.campaign } };\n",
        "mode": "runOnceForEachItem"
      }
    },
    {
      "id": "4cd555b7-177e-5b04-82f0-c7cbd6d0a3ac",
      "name": "Publish",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1200,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "={{ $env.PUBLISH_WEBHOOK_URL }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.PUBLISH_WEBHOOK_KEY || '' }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json) }}",
        "options": {
          "response": {
            "response": {
              "neverError": true,
              "fullResponse": true
            }
          },
          "timeout": 30000
        }
      }
    },
    {
      "id": "9339848a-8f01-5e90-bf10-ffd7a0f883fe",
      "name": "Succeeded",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1440,
        300
      ],
      "parameters": {
        "jsCode": "// Only items the publish endpoint accepted are marked published; failures stay approved\n// and are retried on the next run.\nconst out = [];\n$input.all().forEach((r, i) => {\n  const item = $('Payload').all()[i].json;\n  // A dry run (54-postiz-bridge with DRY_RUN=true) answers 200 but published nothing.\n  const dry = r.json.body && typeof r.json.body === 'object' && r.json.body.status === 'dry_run';\n  if (r.json.statusCode >= 200 && r.json.statusCode < 300 && !dry) {\n    const body = r.json.body || {};\n    out.push({ json: { id: item.id, external_url: (typeof body === 'object' && body.url) || null, short_url: item.short_url } });\n  }\n});\nreturn out;\n"
      }
    },
    {
      "id": "ea95d805-5d09-51e8-8c2b-6823729b1ce7",
      "name": "Mark published",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1680,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "={{ $env.CALENDAR_URL }}/items/{{ $json.id }}/published",
        "options": {},
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $env.INTERNAL_API_KEY }}"
            },
            {
              "name": "X-Approver-Key",
              "value": "={{ $env.APPROVER_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ external_url: $json.external_url, short_url: $json.short_url }) }}"
      }
    }
  ],
  "connections": {
    "Every 15 minutes": {
      "main": [
        [
          {
            "node": "Due items",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Due items": {
      "main": [
        [
          {
            "node": "One per item",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "One per item": {
      "main": [
        [
          {
            "node": "Short link",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Short link": {
      "main": [
        [
          {
            "node": "Payload",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Payload": {
      "main": [
        [
          {
            "node": "Publish",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Publish": {
      "main": [
        [
          {
            "node": "Succeeded",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Succeeded": {
      "main": [
        [
          {
            "node": "Mark published",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "errorWorkflow": "mktWf43ErrorHand"
  },
  "active": false,
  "pinData": {},
  "tags": []
}
