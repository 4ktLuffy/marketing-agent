# Writing model used by the LLM gateway (deploy 03) for copy, briefs and summaries.
# The gateway overrides temperature per prompt; num_ctx must be large enough for
# page text + brand summary + retry feedback.
FROM qwen2.5:7b
PARAMETER num_ctx 8192
PARAMETER temperature 0.7
PARAMETER repeat_penalty 1.1
