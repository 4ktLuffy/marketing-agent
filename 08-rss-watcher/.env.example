# Default feed list used when POST /poll has no "feeds" (one URL per line, # comments)
FEEDS_FILE=/config/feeds.txt
# SQLite file that remembers which items were already returned
DB_PATH=/data/rss.sqlite
# true lets the service fetch private/loopback addresses (only for trusted local testing)
ALLOW_PRIVATE_URLS=false
