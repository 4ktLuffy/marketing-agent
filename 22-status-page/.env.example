# Services to check: "name=url,name=url". A bare name means http://<name>:8000.
# Leave empty to check every service of 01-marketing-stack.
SERVICES=
# Optional: also check Ollama (GET /api/tags).
OLLAMA_URL=http://host.docker.internal:11434
# Seconds per check.
CHECK_TIMEOUT=3
