GATEWAY_URL=http://llm-gateway:8000
BRAND_URL=http://brand-service:8000
KB_URL=http://knowledge-base:8000
# Optional model override for the verification prompts (default: the gateway's model).
VERIFIER_MODEL=
# Strategy for non-numeric claims: base | blind | both
CHECK_MODE=both
