# Required for POST/DELETE /watches (header X-API-Key). Unset = writes return 503.
INTERNAL_API_KEY=
# SQLite file with watches and page snapshots
DB_PATH=/data/monitor.sqlite
# true lets the service fetch private/loopback addresses (only for trusted local testing)
ALLOW_PRIVATE_URLS=false
